#include <stdio.h>
#include <limits.h>

int main()
{
    // Given a number N, and N numbers, find maximum number in these N numbers.
    // <-- Input -->
    // First line contains a number N (1 ≤ N ≤ 103).
    // Second line contains N numbers Xi (0 ≤ Xi ≤ 109).

    // <-- Output -->
    // Print the maximum number.

    // int num;
    // scanf("%d", &num);

    // int v, max = 0;
    // for (int i = 0; i < num; i++)
    // {
    //     scanf("%d", &v);
    //     if (max < v)
    //     {
    //         max = v;
    //     }
    // }
    // printf("%d", max);

    // int num;
    // scanf("%d", &num);

    // int v, max = INT_MIN, min = INT_MAX;
    // for (int i = 0; i < num; i++)
    // {
    //     scanf("%d", &v);
    //     if (max < v)
    //     {
    //         max = v;
    //     }
    //     if (min > v)
    //     {
    //         min = v;
    //     }
    // }
    // printf("%d %d", max, min);

    return 0;
}